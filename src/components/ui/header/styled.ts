import styled from 'styled-components';

import { BASE_INDENT_PX } from '../../constants';

export const Outer = styled.div({
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
  marginBottom: BASE_INDENT_PX
});
