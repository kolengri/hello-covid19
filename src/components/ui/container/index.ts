import styled from 'styled-components';

import { BASE_INDENT_PX } from '../../constants';

export const Container = styled.div({
  marginLeft: 'auto',
  marginRight: 'auto',
  paddingLeft: BASE_INDENT_PX,
  paddingRight: BASE_INDENT_PX
});
